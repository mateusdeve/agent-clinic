---
phase: 03-core-crud
plan: 04
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - frontend/src/app/(dashboard)/agenda/page.tsx
  - frontend/src/components/dashboard/calendar/CalendarDay.tsx
  - frontend/src/components/dashboard/calendar/CalendarWeek.tsx
  - frontend/src/components/dashboard/calendar/CalendarMonth.tsx
  - frontend/src/components/dashboard/calendar/CalendarToolbar.tsx
  - frontend/src/components/dashboard/calendar/AppointmentBlock.tsx
  - frontend/src/app/(dashboard)/agenda/appointment-form.tsx
autonomous: true
requirements: [AGENDA-01, AGENDA-02, AGENDA-03, AGENDA-04, AGENDA-05, AGENDA-06, AGENDA-07]

must_haves:
  truths:
    - "Receptionist sees a calendar with Day/Week/Month toggle"
    - "Day view shows 07:00-20:00 time grid with 30-min rows and appointment blocks positioned by time"
    - "Week view shows 7-column grid with appointment chips per day"
    - "Month view shows 6x7 grid with appointment count per cell and +N more overflow"
    - "Doctor filter dropdown filters visible appointments"
    - "Medico role sees only their own calendar with no doctor filter dropdown"
    - "Clicking empty slot opens slide-over to create appointment"
    - "Clicking appointment block opens slide-over to edit/cancel"
    - "Appointment blocks are color-coded by status per D-03"
  artifacts:
    - path: "frontend/src/app/(dashboard)/agenda/page.tsx"
      provides: "Agenda page with calendar view"
      min_lines: 40
    - path: "frontend/src/components/dashboard/calendar/CalendarDay.tsx"
      provides: "Day view time grid"
      contains: "eachHourOfInterval"
    - path: "frontend/src/components/dashboard/calendar/CalendarWeek.tsx"
      provides: "Week view 7-column grid"
      contains: "startOfWeek"
    - path: "frontend/src/components/dashboard/calendar/CalendarMonth.tsx"
      provides: "Month view 6x7 grid"
      contains: "startOfMonth"
  key_links:
    - from: "frontend/src/app/(dashboard)/agenda/page.tsx"
      to: "/api/appointments"
      via: "apiFetch for appointment data"
      pattern: "apiFetch.*appointments"
    - from: "frontend/src/app/(dashboard)/agenda/page.tsx"
      to: "/api/doctors"
      via: "apiFetch for doctor filter dropdown"
      pattern: "apiFetch.*doctors"
    - from: "frontend/src/app/(dashboard)/agenda/page.tsx"
      to: "/api/doctors/{id}/blocked-slots"
      via: "apiFetch for blocked slot overlay on calendar"
      pattern: "apiFetch.*blocked-slots"
---

<objective>
Custom calendar/agenda page with Day, Week, and Month views. Doctor filter dropdown for receptionists, auto-filtered for medicos. Slide-over forms for creating, editing, and cancelling appointments. Color-coded status blocks.

Purpose: The agenda is the primary daily tool for receptionists and the single view for medicos. It must be scannable, fast, and allow quick scheduling operations.

Output: Fully functional calendar page at /agenda with all 3 views, CRUD operations via slide-over, and role-based filtering.
</objective>

<execution_context>
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-crud/03-CONTEXT.md
@.planning/phases/03-core-crud/03-RESEARCH.md
@.planning/phases/03-core-crud/03-01-SUMMARY.md

@frontend/src/lib/api.ts
@frontend/src/lib/types.ts
@frontend/src/components/dashboard/SlideOver.tsx
@frontend/src/components/dashboard/StatusBadge.tsx
@frontend/src/app/(dashboard)/layout.tsx
@frontend/src/app/globals.css

<interfaces>
From frontend/src/lib/types.ts:
```typescript
export interface Appointment {
  id: string;
  patient_id: string;
  patient_nome: string;
  doctor_id: string;
  doctor_nome: string;
  especialidade: string;
  data_agendamento: string;
  horario: string;
  status: "agendado" | "confirmado" | "realizado" | "cancelado";
  motivo_cancelamento: string | null;
  created_at: string;
}

export interface Doctor {
  id: string;
  nome: string;
  especialidade: string;
  crm: string;
  user_id: string | null;
  is_active: boolean;
}

export interface PaginatedResponse<T> {
  items: T[];
  total: number;
  page: number;
  per_page: number;
}
```

From frontend/src/lib/api.ts:
```typescript
export async function apiFetch<T>(path: string, init?: RequestInit): Promise<T>;
```

From frontend/src/components/dashboard/SlideOver.tsx:
```typescript
export function SlideOver({ open, onClose, title, children }: SlideOverProps);
```

From frontend/src/components/dashboard/StatusBadge.tsx:
```typescript
export function StatusBadge({ status }: { status: AppointmentStatus });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calendar components — Day, Week, Month views + toolbar + appointment blocks</name>
  <files>
    frontend/src/components/dashboard/calendar/CalendarToolbar.tsx
    frontend/src/components/dashboard/calendar/AppointmentBlock.tsx
    frontend/src/components/dashboard/calendar/CalendarDay.tsx
    frontend/src/components/dashboard/calendar/CalendarWeek.tsx
    frontend/src/components/dashboard/calendar/CalendarMonth.tsx
  </files>
  <read_first>
    frontend/src/lib/types.ts
    frontend/src/components/dashboard/StatusBadge.tsx
    frontend/src/app/globals.css
  </read_first>
  <action>
All calendar components are "use client". Import date-fns functions with `import { ptBR } from "date-fns/locale"` for Portuguese day/month names.

Per D-02: Custom calendar with date-fns + Tailwind. No FullCalendar. Full brand control with green palette.

1. **CalendarToolbar.tsx** — Navigation and view toggle
   Props: currentDate (Date), view ("day"|"week"|"month"), onDateChange ((d: Date) => void), onViewChange ((v: string) => void), doctors (Doctor[]), selectedDoctorId (string|null), onDoctorChange ((id: string|null) => void), showDoctorFilter (boolean)
   - Left: Anterior/Hoje/Proximo navigation buttons (ChevronLeft, ChevronRight from lucide-react)
   - Center: Current date label — Day: "Sexta, 29 de marco de 2026", Week: "24-30 Mar 2026", Month: "Marco 2026" — using `format(date, pattern, { locale: ptBR })`
   - Right: Dia/Semana/Mes toggle buttons (active state with green-500 bg)
   - Below (if showDoctorFilter): Doctor filter dropdown using shadcn Select. "Todos os medicos" default + each doctor.nome

2. **AppointmentBlock.tsx** — Single appointment chip
   Props: appointment (Appointment), compact (boolean, for month view), onClick (() => void)
   - Per D-03 color mapping:
     - agendado: bg-blue-50 border-l-4 border-blue-500 text-blue-700
     - confirmado: bg-green-50 border-l-4 border-green-500 text-green-700
     - realizado: bg-gray-50 border-l-4 border-gray-400 text-gray-500
     - cancelado: bg-red-50 border-l-4 border-red-400 text-red-400 line-through
   - Full: shows horario + patient_nome + doctor_nome
   - Compact: shows just horario + patient_nome (truncated)
   - cursor-pointer, hover:shadow-sm transition

3. **CalendarDay.tsx** — Day view with time grid
   Props: date (Date), appointments (Appointment[]), onSlotClick ((time: string) => void), onAppointmentClick ((a: Appointment) => void, blockedSlots (BlockedSlot[]))
   - Time grid: 07:00 to 20:00, 30-minute rows (per research recommendation)
   - Left column: time labels (07:00, 07:30, 08:00, ...)
   - Right column: relative positioned container. Appointment blocks absolutely positioned based on time offset: `top = ((hour - 7) * 2 + (minute / 30)) * ROW_HEIGHT`
   - Each row has a subtle border-b border-gray-100
   - Filter appointments for current date: `isSameDay(parseISO(a.data_agendamento), date)`
   - Empty rows are clickable — onClick passes the slot time to onSlotClick
   - Use `eachHourOfInterval`, `setHours`, `setMinutes` from date-fns for time generation
   - Current time indicator: red horizontal line at current time position (only if date isToday())
   - Blocked slot rendering (AGENDA-06): Accept blockedSlots prop. For each BlockedSlot whose blocked_date matches the current date, render a semi-transparent gray band (bg-gray-200/60) spanning from start_time to end_time (or 07:00-20:00 if null). Position using same absolute positioning formula as appointments. Add a small "Bloqueado" or reason text in muted gray. Blocked bands appear behind appointment blocks (lower z-index).

4. **CalendarWeek.tsx** — Week view 7-column grid
   Props: date (Date), appointments (Appointment[]), onDayClick ((d: Date) => void), onAppointmentClick ((a: Appointment) => void, blockedSlots (BlockedSlot[]))
   - Header row: day names (Seg, Ter, ..., Dom) with date number
   - 7 columns using `eachDayOfInterval({ start: startOfWeek(date, { locale: ptBR }), end: endOfWeek(date, { locale: ptBR }) })`
   - Each column: list of AppointmentBlock (compact=false) for that day
   - Today's column highlighted with green-50 background
   - Clicking day header switches to day view (via onDayClick)

5. **CalendarMonth.tsx** — Month view 6x7 grid
   Props: date (Date), appointments (Appointment[]), onDayClick ((d: Date) => void), onAppointmentClick ((a: Appointment) => void, blockedSlots (BlockedSlot[]))
   - Header row: day abbreviations (D, S, T, Q, Q, S, S)
   - Calculate grid: startOfMonth → endOfMonth, padded to full weeks
   - Each cell: date number (gray if outside current month), up to 3 AppointmentBlock (compact=true), "+N mais" link if overflow
   - Today's cell: ring-2 ring-green-500
   - Clicking "+N mais" or day number switches to day view
  </action>
  <verify>
    <automated>cd /Users/mateuspires/Dev/agent-clinic/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All 5 component files exist under `frontend/src/components/dashboard/calendar/`
    - CalendarDay.tsx contains "eachHourOfInterval" or equivalent time generation logic
    - CalendarWeek.tsx contains "startOfWeek" and "eachDayOfInterval"
    - CalendarMonth.tsx contains "startOfMonth" and "endOfMonth"
    - AppointmentBlock.tsx contains color mappings for all 4 statuses: "agendado", "confirmado", "realizado", "cancelado"
    - CalendarToolbar.tsx contains "Dia", "Semana", "Mes" toggle labels (pt-BR)
    - All components have "use client" directive
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Five calendar components render Day/Week/Month views with date-fns date math, color-coded appointment blocks, and Portuguese locale.</done>
</task>

<task type="auto">
  <name>Task 2: Agenda page — wire calendar views, doctor filter, appointment slide-over CRUD</name>
  <files>
    frontend/src/app/(dashboard)/agenda/page.tsx
    frontend/src/app/(dashboard)/agenda/appointment-form.tsx
  </files>
  <read_first>
    frontend/src/lib/api.ts
    frontend/src/lib/types.ts
    frontend/src/components/dashboard/SlideOver.tsx
    frontend/src/components/dashboard/calendar/CalendarToolbar.tsx
    frontend/src/components/dashboard/calendar/CalendarDay.tsx
    frontend/src/components/dashboard/calendar/CalendarWeek.tsx
    frontend/src/components/dashboard/calendar/CalendarMonth.tsx
    frontend/src/app/(dashboard)/layout.tsx
  </read_first>
  <action>
1. Create `frontend/src/app/(dashboard)/agenda/appointment-form.tsx` — "use client"
   - Reusable form for create and edit modes
   - Props: appointment (Appointment | null for create), patients (list for dropdown), doctors (list for dropdown), onSubmit, onCancel, onDelete (optional, for edit mode)
   - Fields: Paciente (searchable select), Medico (select), Data (date input), Horario (time input from available slots), Especialidade (auto-filled from doctor)
   - react-hook-form + zod validation (per D-08):
     - patient_id: required
     - doctor_id: required
     - data_agendamento: required, must be today or future
     - horario: required, "HH:MM" format
   - Submit button: "Agendar" (create) or "Salvar" (edit)
   - In edit mode: show StatusBadge for current status, show "Cancelar Consulta" button with confirmation dialog (AGENDA-04). Cancellation prompts for optional motivo text.
   - Status change buttons: if status is agendado, show "Confirmar" button. If confirmado, show "Marcar como Realizado" button. These call PATCH /appointments/{id}/status.

2. Create `frontend/src/app/(dashboard)/agenda/page.tsx` — "use client"
   - State: currentDate (Date), view ("day"|"week"|"month"), selectedDoctorId (string|null), slideOverOpen (boolean), selectedAppointment (Appointment|null), appointments (Appointment[]), doctors (Doctor[]), patients (for form dropdown)
   - On mount: fetch doctors list via `apiFetch<PaginatedResponse<Doctor>>("/api/doctors?per_page=100")`
   - On mount + when date/view/doctor changes: fetch appointments via `apiFetch<PaginatedResponse<Appointment>>("/api/appointments?date_from=X&date_to=Y&doctor_id=Z&per_page=500")`. Date range depends on view: day=single day, week=week bounds, month=month bounds.
   - AGENDA-06 blocked slots: When a doctor is selected (selectedDoctorId is not null), also fetch blocked slots via `apiFetch<BlockedSlot[]>("/api/doctors/${selectedDoctorId}/blocked-slots")`. Store in state as blockedSlots (BlockedSlot[]). Pass blockedSlots to CalendarDay and CalendarWeek components as a prop. In CalendarDay.tsx, render blocked time windows as gray bands (bg-gray-200/50 with diagonal stripe pattern or opacity) overlaid on the time grid rows. The bands span from start_time to end_time (or full day if both null). In CalendarWeek.tsx, show a small gray indicator for days with blocks. This ensures "blocked slots reflected in calendar display" per AGENDA-06 success criteria.
   - AGENDA-07 medico isolation: Read user role from NextAuth session via `useSession()` from "next-auth/react". If role === "medico", hide the doctor filter dropdown (showDoctorFilter=false). The API already enforces medico filtering server-side (plan 03-02), so no additional client filtering needed. Just hide the UI control.
   - Render CalendarToolbar + conditional CalendarDay/CalendarWeek/CalendarMonth based on view state
   - onSlotClick: open slide-over with empty appointment form, pre-fill date and time from clicked slot
   - onAppointmentClick: open slide-over with appointment data pre-loaded for editing
   - After form submit (create/edit/cancel/status change): close slide-over, refetch appointments
   - Loading state: show skeleton calendar grid while fetching
   - All text in pt-BR: "Agenda", "Novo Agendamento", etc.
  </action>
  <verify>
    <automated>cd /Users/mateuspires/Dev/agent-clinic/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/(dashboard)/agenda/page.tsx` exists with "use client"
    - Page imports and renders CalendarToolbar, CalendarDay, CalendarWeek, CalendarMonth conditionally
    - Page fetches from "/api/appointments" and "/api/doctors" via apiFetch
    - Page checks session role for medico to hide doctor filter (AGENDA-07)
    - `appointment-form.tsx` exists with react-hook-form + zod validation
    - Form has fields for patient, doctor, date, time
    - Form has cancel button with motivo prompt (AGENDA-04)
    - Form has status transition buttons (AGENDA-05)
    - Page fetches blocked slots from "/api/doctors/{id}/blocked-slots" when doctor is selected
    - CalendarDay.tsx accepts blockedSlots prop and renders gray overlay bands
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Agenda page renders calendar with 3 views, doctor filter, medico isolation, and slide-over CRUD for appointments with status management.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Agenda page exists at correct route path
- Calendar components use date-fns (not moment.js, not FullCalendar)
- Color coding matches D-03 exactly
- Medico role check present for doctor filter visibility
</verification>

<success_criteria>
- Calendar page at /agenda with Day/Week/Month toggle (AGENDA-01)
- Create appointment via slide-over (AGENDA-02)
- Edit/reschedule via slide-over (AGENDA-03)
- Cancel with optional motivo (AGENDA-04)
- Status tracking with color coding (AGENDA-05)
- Doctor filter dropdown (AGENDA-01), hidden for medico (AGENDA-07)
- Blocked slots reflected in calendar display (AGENDA-06)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-crud/03-04-SUMMARY.md`
</output>
