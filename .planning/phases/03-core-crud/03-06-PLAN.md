---
phase: 03-core-crud
plan: 06
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - frontend/src/app/(dashboard)/medicos/page.tsx
  - frontend/src/app/(dashboard)/medicos/columns.tsx
  - frontend/src/app/(dashboard)/medicos/doctor-form.tsx
  - frontend/src/app/(dashboard)/medicos/schedule-grid.tsx
  - frontend/src/app/(dashboard)/usuarios/page.tsx
  - frontend/src/app/(dashboard)/usuarios/columns.tsx
  - frontend/src/app/(dashboard)/usuarios/user-form.tsx
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, USER-01, USER-02, USER-03, USER-04, USER-05]

must_haves:
  truths:
    - "Admin sees paginated doctor list with specialty column"
    - "Admin can create/edit doctor profile via slide-over with nome, especialidade, CRM fields"
    - "Admin can define weekly availability grid per doctor (day x time slots)"
    - "Admin sees paginated user list with role column"
    - "Admin can create user with role assignment via slide-over"
    - "Admin can change user role inline or via form"
    - "Admin can activate/deactivate user with toggle"
    - "Admin can reset user password"
  artifacts:
    - path: "frontend/src/app/(dashboard)/medicos/page.tsx"
      provides: "Doctor list page"
      contains: "DataTable"
    - path: "frontend/src/app/(dashboard)/medicos/schedule-grid.tsx"
      provides: "Weekly availability grid editor"
      contains: "day_of_week"
    - path: "frontend/src/app/(dashboard)/usuarios/page.tsx"
      provides: "User management page"
      contains: "DataTable"
  key_links:
    - from: "frontend/src/app/(dashboard)/medicos/page.tsx"
      to: "/api/doctors"
      via: "apiFetch for doctor CRUD"
      pattern: "apiFetch.*doctors"
    - from: "frontend/src/app/(dashboard)/usuarios/page.tsx"
      to: "/api/users"
      via: "apiFetch for user management"
      pattern: "apiFetch.*users"
---

<objective>
Admin-only management pages for doctors and users. Doctor page includes list, create/edit slide-over, and weekly availability grid editor. User page includes list, create slide-over, role change, activate/deactivate toggle, and password reset.

Purpose: Admin tools for managing clinic staff and system access. Doctor availability is a prerequisite for correct appointment scheduling.

Output: Doctor management page at /medicos and user management page at /usuarios.
</objective>

<execution_context>
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-crud/03-CONTEXT.md
@.planning/phases/03-core-crud/03-RESEARCH.md
@.planning/phases/03-core-crud/03-01-SUMMARY.md

@frontend/src/lib/api.ts
@frontend/src/lib/types.ts
@frontend/src/components/dashboard/DataTable.tsx
@frontend/src/components/dashboard/SlideOver.tsx
@frontend/src/app/(dashboard)/layout.tsx

<interfaces>
From frontend/src/lib/types.ts:
```typescript
export interface Doctor {
  id: string; nome: string; especialidade: string;
  crm: string; user_id: string | null; is_active: boolean;
}
export interface DoctorSchedule {
  id: string; doctor_id: string;
  day_of_week: number; start_time: string; end_time: string;
}
export interface SystemUser {
  id: string; email: string; name: string;
  role: "admin" | "recepcionista" | "medico";
  is_active: boolean; created_at: string;
}
export interface PaginatedResponse<T> { items: T[]; total: number; page: number; per_page: number; }
```

From frontend/src/components/dashboard/DataTable.tsx:
```typescript
export function DataTable<TData>({ columns, data, pageCount, pagination, onPaginationChange, isLoading });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Doctor management page — list, create/edit form, availability schedule grid</name>
  <files>
    frontend/src/app/(dashboard)/medicos/page.tsx
    frontend/src/app/(dashboard)/medicos/columns.tsx
    frontend/src/app/(dashboard)/medicos/doctor-form.tsx
    frontend/src/app/(dashboard)/medicos/schedule-grid.tsx
  </files>
  <read_first>
    frontend/src/lib/api.ts
    frontend/src/lib/types.ts
    frontend/src/components/dashboard/DataTable.tsx
    frontend/src/components/dashboard/SlideOver.tsx
  </read_first>
  <action>
1. Create `frontend/src/app/(dashboard)/medicos/columns.tsx` — "use client"
   - ColumnDef<Doctor>[]:
     - nome: header "Medico"
     - especialidade: header "Especialidade"
     - crm: header "CRM"
     - actions: "Editar" button (opens slide-over), "Horarios" button (opens schedule grid)

2. Create `frontend/src/app/(dashboard)/medicos/doctor-form.tsx` — "use client"
   - Per D-08: react-hook-form + zod
   - Props: doctor (Doctor | null), onSubmit, onCancel
   - Fields:
     - nome: text, required
     - especialidade: text, required (could use a select with common specialties: Clinico Geral, Cardiologia, Dermatologia, Ortopedia, Pediatria, Ginecologia, Neurologia, Oftalmologia, Otorrinolaringologia, Psiquiatria — but also allow free text)
     - crm: text, required, pattern "CRM/XX 123456"
     - user_id: optional select populated with existing users with role "medico" — for linking a Medico user to this doctor profile (AGENDA-07 linkage)
   - Submit: "Cadastrar" or "Salvar"

3. Create `frontend/src/app/(dashboard)/medicos/schedule-grid.tsx` — "use client"
   - Per DOC-03: Weekly availability grid editor
   - Props: doctorId (string), onClose (() => void)
   - On mount: fetch existing schedule via `apiFetch<DoctorSchedule[]>("/api/doctors/${doctorId}/schedules")`
   - Grid layout: 7 columns (Seg, Ter, Qua, Qui, Sex, Sab, Dom) x time rows (07:00 to 20:00, 30-min slots)
   - Each cell is clickable to toggle availability:
     - Active (available): bg-green-100 border-green-400
     - Inactive (unavailable): bg-white border-gray-200
   - Click and drag to select multiple cells (or simple click per cell)
   - "Salvar Horarios" button: collect all active cells as ScheduleSlot[], PUT /api/doctors/{doctorId}/schedules with { slots: [...] }
   - The grid converts internal state to ScheduleSlot format: for each day with consecutive active cells, create one slot with start_time (first active) and end_time (last active + 30min)
   - Loading: skeleton grid while fetching

4. Create `frontend/src/app/(dashboard)/medicos/page.tsx` — "use client"
   - Page title: "Medicos" with "Novo Medico" button
   - State: doctors, pagination, slideOverOpen, selectedDoctor, scheduleGridOpen, scheduleGridDoctorId
   - Fetch: `apiFetch<PaginatedResponse<Doctor>>("/api/doctors?page=${page}&per_page=20")`
   - DataTable with doctor columns
   - "Novo Medico" / "Editar" open SlideOver with DoctorForm
   - "Horarios" opens SlideOver (wider, max-w-2xl) with ScheduleGrid
   - On form submit: POST/PUT /api/doctors, close slide-over, refetch
   - All text pt-BR
  </action>
  <verify>
    <automated>cd /Users/mateuspires/Dev/agent-clinic/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/(dashboard)/medicos/page.tsx` exists with "use client"
    - Page renders DataTable with doctor columns (nome, especialidade, crm, actions)
    - `doctor-form.tsx` has react-hook-form + zod with fields nome, especialidade, crm, user_id
    - `schedule-grid.tsx` renders 7-column availability grid with clickable cells
    - Schedule grid fetches from /api/doctors/{id}/schedules and saves via PUT
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Doctor management page at /medicos with list, create/edit form, and weekly availability grid editor.</done>
</task>

<task type="auto">
  <name>Task 2: User management page — list, create, role change, activate/deactivate, password reset</name>
  <files>
    frontend/src/app/(dashboard)/usuarios/page.tsx
    frontend/src/app/(dashboard)/usuarios/columns.tsx
    frontend/src/app/(dashboard)/usuarios/user-form.tsx
  </files>
  <read_first>
    frontend/src/lib/api.ts
    frontend/src/lib/types.ts
    frontend/src/components/dashboard/DataTable.tsx
    frontend/src/components/dashboard/SlideOver.tsx
    frontend/src/components/ui/dialog.tsx
  </read_first>
  <action>
1. Create `frontend/src/app/(dashboard)/usuarios/columns.tsx` — "use client"
   - ColumnDef<SystemUser>[]:
     - name: header "Nome"
     - email: header "Email"
     - role: header "Perfil", cell renders role with colored badge (admin=purple, recepcionista=blue, medico=green)
     - is_active: header "Status", cell renders "Ativo" green badge or "Inativo" gray badge
     - actions: "Editar" button, "Redefinir Senha" button

2. Create `frontend/src/app/(dashboard)/usuarios/user-form.tsx` — "use client"
   - Per D-08: react-hook-form + zod
   - Two modes: create (with password field) and edit (without password, only role change)
   - Props: user (SystemUser | null), mode ("create" | "edit"), onSubmit, onCancel
   - Create mode fields:
     - name: text, required
     - email: email, required
     - password: password, required, min 6 chars
     - role: select with options: Admin, Recepcionista, Medico
   - Edit mode fields:
     - name: text (read-only display)
     - email: text (read-only display)
     - role: select — changeable (USER-03)
   - Submit: "Criar Usuario" or "Salvar"

3. Create `frontend/src/app/(dashboard)/usuarios/page.tsx` — "use client"
   - Page title: "Usuarios" with "Novo Usuario" button
   - State: users, pagination, slideOverOpen, selectedUser, formMode, passwordResetDialogOpen, passwordResetUserId
   - Fetch: `apiFetch<PaginatedResponse<SystemUser>>("/api/users?page=${page}&per_page=20")`
   - DataTable with user columns
   - "Novo Usuario" opens SlideOver with UserForm mode="create"
   - "Editar" opens SlideOver with UserForm mode="edit"
   - On create submit: POST /api/users, close, refetch
   - On edit submit: PATCH /api/users/{id}/role, close, refetch

   - Inline status toggle (USER-04):
     - In the is_active column cell, render a toggle switch
     - On toggle: show confirmation dialog "Desativar usuario {name}?" or "Reativar usuario {name}?"
     - On confirm: PATCH /api/users/{id}/status with { is_active: newValue }, refetch
     - Prevent deactivating yourself: if user.id matches current session user id, disable toggle

   - Password reset (USER-05):
     - "Redefinir Senha" button in actions column
     - Opens a dialog (shadcn Dialog) with a new password input field + confirm button
     - On submit: POST /api/users/{id}/reset-password with { new_password: value }
     - Show success toast or inline message "Senha redefinida com sucesso"
     - Per research: Admin-managed reset only. No email/self-service in Phase 3.

   - All text pt-BR
  </action>
  <verify>
    <automated>cd /Users/mateuspires/Dev/agent-clinic/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/(dashboard)/usuarios/page.tsx` exists with "use client"
    - Page renders DataTable with user columns (name, email, role, is_active, actions)
    - `user-form.tsx` has create mode (with password) and edit mode (role only)
    - Page has inline status toggle with confirmation dialog
    - Page has password reset dialog
    - Self-deactivation prevention: toggle disabled for current user
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>User management page at /usuarios with create, role change, activate/deactivate toggle, and admin password reset.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Doctor page at /medicos with DataTable and availability grid
- User page at /usuarios with DataTable, role management, and password reset
- Both pages are admin-only (sidebar already restricts visibility per layout.tsx)
</verification>

<success_criteria>
- Doctor list with specialty (DOC-01)
- Create/edit doctor with nome, especialidade, CRM (DOC-02)
- Weekly availability grid editor (DOC-03)
- User list with role (USER-01)
- Create user with role (USER-02)
- Change role (USER-03)
- Activate/deactivate (USER-04)
- Reset password (USER-05)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-crud/03-06-SUMMARY.md`
</output>
