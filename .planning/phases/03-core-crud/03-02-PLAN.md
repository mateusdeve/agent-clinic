---
phase: 03-core-crud
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - agent-service/src/api/patients.py
  - agent-service/src/api/appointments.py
  - agent-service/src/api/webhook.py
autonomous: true
requirements: [API-01, PAT-01, PAT-02, PAT-03, PAT-04, PAT-05, AGENDA-02, AGENDA-03, AGENDA-04, AGENDA-05, AGENDA-07]

must_haves:
  truths:
    - "GET /api/patients returns paginated patient list with search by name/phone"
    - "POST /api/patients creates a new patient in the correct tenant"
    - "PUT /api/patients/{id} updates patient data"
    - "GET /api/patients/{id} returns single patient by ID or 404"
    - "GET /api/patients/{id}/appointments returns appointment history for a patient"
    - "GET /api/patients/{id}/conversations returns WhatsApp conversation messages"
    - "POST /api/appointments creates an appointment with status=agendado"
    - "PUT /api/appointments/{id} edits an existing appointment"
    - "PATCH /api/appointments/{id}/cancel cancels with optional motivo"
    - "GET /api/appointments returns appointments filterable by doctor_id and date range"
    - "Medico role can only see their own appointments via doctor_id enforcement"
  artifacts:
    - path: "agent-service/src/api/patients.py"
      provides: "Patient CRUD router"
      exports: ["router"]
    - path: "agent-service/src/api/appointments.py"
      provides: "Appointment CRUD router"
      exports: ["router"]
  key_links:
    - from: "agent-service/src/api/patients.py"
      to: "agent-service/src/api/deps.py"
      via: "Depends(get_db_for_tenant)"
      pattern: "Depends\\(get_db_for_tenant\\)"
    - from: "agent-service/src/api/appointments.py"
      to: "agent-service/src/api/deps.py"
      via: "Depends(require_role) + Depends(get_current_user) for medico filtering"
      pattern: "require_role|get_current_user"
---

<objective>
FastAPI REST routers for patients and appointments — the two most data-heavy entities. Patients router covers list/search, create, edit, appointment history, and WhatsApp conversation history. Appointments router covers create, edit, cancel, status tracking, and medico-only filtering.

Purpose: These two routers serve both the patient management pages and the calendar/agenda pages in the frontend. They form the core data backbone of the CRUD phase.

Output: Two mounted FastAPI routers with full CRUD operations, pagination, and role-based access control.
</objective>

<execution_context>
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-crud/03-CONTEXT.md
@.planning/phases/03-core-crud/03-RESEARCH.md

@agent-service/src/api/auth.py
@agent-service/src/api/deps.py
@agent-service/src/api/webhook.py
@agent-service/alembic/versions/004_crud_schema_additions.py
@agent-service/src/tools/patients.py
@agent-service/src/tools/appointments.py
@agent-service/src/memory/persistence.py

<interfaces>
From agent-service/src/api/deps.py:
```python
def get_current_user(token) -> dict:  # returns {"user_id", "tenant_id", "role"}
def get_current_tenant(user) -> str:  # returns tenant_id
def require_role(*roles):             # factory returning dependency
def get_db_for_tenant(tenant_id):     # yields psycopg2 conn with SET LOCAL
```

Pagination envelope (per D-12):
```python
class PaginatedResponse(BaseModel):
    items: list
    total: int
    page: int
    per_page: int
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patients API router — list, create, edit, appointment history, conversation history</name>
  <files>agent-service/src/api/patients.py</files>
  <read_first>
    agent-service/src/api/auth.py
    agent-service/src/api/deps.py
    agent-service/src/tools/patients.py
    agent-service/src/memory/persistence.py
    agent-service/alembic/versions/004_crud_schema_additions.py
  </read_first>
  <action>
Create `agent-service/src/api/patients.py` with APIRouter(prefix="/api/patients", tags=["patients"]).

Per D-14: Do NOT import or call functions from src/tools/patients.py. Write new SQL queries using get_db_for_tenant (RLS handles tenant isolation automatically).

Per D-13: All endpoints use Depends(get_db_for_tenant) + Depends(require_role(...)).

Logger: `logging.getLogger("agent-clinic.api.patients")` with `[patients]` prefix in messages.

Pydantic models:
- PatientCreate(BaseModel): nome (str), phone (str), data_nascimento (Optional[str] = None, ISO date), notas (Optional[str] = None)
- PatientUpdate(BaseModel): nome (Optional[str]), phone (Optional[str]), data_nascimento (Optional[str]), notas (Optional[str])
- PatientOut(BaseModel): id (str), phone (str), nome (str), data_nascimento (Optional[str]), notas (Optional[str]), total_consultas (int), created_at (str)

Endpoints:

1. **GET /** — List patients with search (PAT-01)
   - Dependencies: require_role("admin", "recepcionista")
   - Query params: search (Optional[str]), page (int=1, ge=1), per_page (int=20, ge=1, le=100)
   - SQL: SELECT id, phone, nome, data_nascimento, notas, (SELECT COUNT(*) FROM appointments WHERE appointments.patient_id = patients.id) as total_consultas, created_at FROM patients
   - WHERE: if search provided, filter by `LOWER(nome) LIKE LOWER(%s) OR phone LIKE %s` with `%search%` pattern
   - ORDER BY nome ASC, LIMIT per_page OFFSET (page-1)*per_page
   - Count query for total
   - Return PaginatedResponse envelope (per D-12)

2. **POST /** — Create patient (PAT-02)
   - Dependencies: require_role("admin", "recepcionista")
   - Body: PatientCreate
   - SQL: INSERT INTO patients (phone, nome, data_nascimento, notas, tenant_id) VALUES (%s, %s, %s, %s, (SELECT current_setting('app.tenant_id')::uuid))
   - Note: tenant_id is inserted via current_setting because RLS is USING not WITH CHECK on insert — include explicit tenant_id from current_setting
   - RETURNING id, phone, nome, data_nascimento, notas, created_at
   - conn.commit() after insert
   - Return created patient with 201 status

3. **GET /{patient_id}** — Single patient by ID
   - Dependencies: require_role("admin", "recepcionista")
   - SQL: SELECT id, phone, nome, data_nascimento, notas, (SELECT COUNT(*) FROM appointments WHERE appointments.patient_id = patients.id) as total_consultas, created_at FROM patients WHERE id = %s
   - 404 if no row returned
   - Return PatientOut object

4. **PUT /{patient_id}** — Edit patient (PAT-03)
   - Dependencies: require_role("admin", "recepcionista")
   - Body: PatientUpdate
   - Build SET clause dynamically from non-None fields only
   - SQL: UPDATE patients SET ... WHERE id = %s RETURNING id, phone, nome, data_nascimento, notas, created_at
   - 404 if no row returned
   - conn.commit()

5. **GET /{patient_id}/appointments** — Patient appointment history (PAT-04)
   - Dependencies: require_role("admin", "recepcionista")
   - SQL: SELECT a.id, a.data_agendamento, a.horario_agendamento as horario, a.status, d.nome as doctor_nome, d.especialidade FROM appointments a LEFT JOIN doctors d ON a.doctor_id = d.id WHERE a.patient_id = %s ORDER BY a.data_agendamento DESC, a.horario_agendamento DESC
   - Note: Read existing appointments table column names from src/tools/appointments.py to get exact column names (may be data_agendamento/horario_agendamento or similar). Adapt SQL accordingly.
   - Return list (no pagination needed — patient history is bounded)

6. **GET /{patient_id}/conversations** — WhatsApp conversation history (PAT-05)
   - Dependencies: require_role("admin", "recepcionista")
   - Read src/memory/persistence.py to find exact conversations table schema (columns: session_id, role, content, created_at, metadata)
   - First look up patient phone: SELECT phone FROM patients WHERE id = %s
   - Then: SELECT id, role, content, created_at FROM conversations WHERE session_id LIKE %phone% ORDER BY created_at ASC
   - Note: session_id in conversations table likely contains the phone number. Read persistence.py to confirm the exact pattern. If session_id format is different, adapt the query.
   - Return list of ConversationMessage objects

Error handling per CLAUDE.md: wrap each endpoint body in try/except Exception, logger.error(f"[patients] message: {e}"), raise HTTPException(500).
  </action>
  <verify>
    <automated>cd /Users/mateuspires/Dev/agent-clinic/agent-service && .venv/bin/python -c "from src.api.patients import router; print(f'Routes: {len(router.routes)}')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `agent-service/src/api/patients.py` exists and exports `router`
    - File contains 6 route decorators: GET /, GET /{patient_id}, POST /, PUT /{patient_id}, GET /{patient_id}/appointments, GET /{patient_id}/conversations
    - Every endpoint uses Depends(get_db_for_tenant)
    - Every endpoint uses Depends(require_role(...))
    - File contains PaginatedResponse usage for GET / endpoint
    - File does NOT import from src.tools.patients
    - Logger initialized as `logging.getLogger("agent-clinic.api.patients")`
    - Python import succeeds without errors
  </acceptance_criteria>
  <done>Patients router has 6 endpoints (list, get by ID, create, edit, appointment history, conversation history) covering PAT-01 through PAT-05, all tenant-isolated via RLS, role-protected, with pagination on list endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Appointments API router — list, create, edit, cancel, medico filtering + mount both routers</name>
  <files>
    agent-service/src/api/appointments.py
    agent-service/src/api/webhook.py
  </files>
  <read_first>
    agent-service/src/api/deps.py
    agent-service/src/tools/appointments.py
    agent-service/src/tools/doctors.py
    agent-service/src/api/webhook.py
  </read_first>
  <action>
Create `agent-service/src/api/appointments.py` with APIRouter(prefix="/api/appointments", tags=["appointments"]).

Per D-14: Do NOT import or call functions from src/tools/appointments.py. Write new SQL queries.

Logger: `logging.getLogger("agent-clinic.api.appointments")`

Pydantic models:
- AppointmentCreate(BaseModel): patient_id (str), doctor_id (str), data_agendamento (str, ISO date), horario (str, "HH:MM"), especialidade (Optional[str] = None)
- AppointmentUpdate(BaseModel): doctor_id (Optional[str]), data_agendamento (Optional[str]), horario (Optional[str]), especialidade (Optional[str])
- AppointmentCancel(BaseModel): motivo (Optional[str] = None)
- AppointmentOut(BaseModel): id, patient_id, patient_nome, doctor_id, doctor_nome, especialidade, data_agendamento, horario, status, motivo_cancelamento, created_at

Endpoints:

1. **GET /** — List appointments with filters (AGENDA-01 data source)
   - Dependencies: get_current_user (not just require_role — need user dict for medico check)
   - Roles: admin, recepcionista, medico (all can access)
   - Query params: doctor_id (Optional[str]), date_from (Optional[str]), date_to (Optional[str]), status (Optional[str]), page (int=1), per_page (int=20)
   - **AGENDA-07 medico isolation:** If current_user["role"] == "medico", look up the doctor record linked to this user: `SELECT id FROM doctors WHERE user_id = %s` using current_user["user_id"]. Override doctor_id filter with this value. If no linked doctor found, return empty results (don't expose other doctors' data).
   - SQL: SELECT a.*, p.nome as patient_nome, d.nome as doctor_nome, d.especialidade FROM appointments a JOIN patients p ON a.patient_id = p.id JOIN doctors d ON a.doctor_id = d.id WHERE 1=1 [+ dynamic filters] ORDER BY a.data_agendamento, a.horario_agendamento LIMIT/OFFSET
   - Read src/tools/appointments.py to confirm exact column names (data_agendamento, horario_agendamento, etc.)
   - Return PaginatedResponse

2. **POST /** — Create appointment (AGENDA-02)
   - Dependencies: require_role("admin", "recepcionista")
   - Body: AppointmentCreate
   - SQL: INSERT INTO appointments (patient_id, doctor_id, data_agendamento, horario_agendamento, especialidade, status, tenant_id) VALUES (%s, %s, %s, %s, %s, 'agendado', current_setting('app.tenant_id')::uuid)
   - AGENDA-05: New appointments always start as 'agendado'
   - RETURNING *
   - conn.commit()
   - Return created appointment with 201

3. **PUT /{appointment_id}** — Edit/reschedule appointment (AGENDA-03)
   - Dependencies: require_role("admin", "recepcionista")
   - Body: AppointmentUpdate
   - Dynamic SET clause from non-None fields
   - 404 if no row returned
   - conn.commit()

4. **PATCH /{appointment_id}/cancel** — Cancel appointment (AGENDA-04)
   - Dependencies: require_role("admin", "recepcionista")
   - Body: AppointmentCancel (optional motivo)
   - SQL: UPDATE appointments SET status = 'cancelado', motivo_cancelamento = %s WHERE id = %s AND status != 'cancelado' RETURNING *
   - Note: If appointments table doesn't have motivo_cancelamento column, add it in migration 004. Check current schema first.
   - 404 if no row (already cancelled or doesn't exist)
   - conn.commit()

5. **PATCH /{appointment_id}/status** — Update status (AGENDA-05)
   - Dependencies: require_role("admin", "recepcionista")
   - Body: { status: str } — must be one of agendado, confirmado, realizado, cancelado
   - Validate status is in allowed values
   - SQL: UPDATE appointments SET status = %s WHERE id = %s RETURNING *
   - conn.commit()

After creating both routers, update `agent-service/src/api/webhook.py`:
- Add imports: `from src.api.patients import router as patients_router` and `from src.api.appointments import router as appointments_router`
- Add: `app.include_router(patients_router)` and `app.include_router(appointments_router)`
- Place after the existing `app.include_router(auth_router)` line

Error handling: try/except Exception per endpoint, logger.error with [appointments] prefix.
  </action>
  <verify>
    <automated>cd /Users/mateuspires/Dev/agent-clinic/agent-service && .venv/bin/python -c "from src.api.appointments import router; print(f'Routes: {len(router.routes)}'); from src.api.webhook import app; print(f'App routes: {len(app.routes)}')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `agent-service/src/api/appointments.py` exists and exports `router`
    - File contains 5 route decorators: GET /, POST /, PUT /{appointment_id}, PATCH /{appointment_id}/cancel, PATCH /{appointment_id}/status
    - GET / contains medico isolation logic: checks role == "medico" and overrides doctor_id from doctors.user_id lookup
    - POST / sets status = 'agendado' on creation
    - PATCH /cancel sets status = 'cancelado'
    - `agent-service/src/api/webhook.py` contains `include_router(patients_router)` and `include_router(appointments_router)`
    - Python import of both routers succeeds
    - File does NOT import from src.tools.appointments
  </acceptance_criteria>
  <done>Appointments router has 5 endpoints with medico isolation (AGENDA-07), status tracking (AGENDA-05), and both routers are mounted on the FastAPI app.</done>
</task>

</tasks>

<verification>
- Both routers import without error: `python -c "from src.api.patients import router; from src.api.appointments import router"`
- webhook.py includes both routers
- Medico role enforcement present in appointments GET endpoint
- All endpoints use get_db_for_tenant dependency
</verification>

<success_criteria>
- Patients router: 6 endpoints (list+search, get by ID, create, edit, appointment history, conversation history)
- Appointments router: 5 endpoints (list+filter, create, edit, cancel, status update)
- Both mounted on FastAPI app
- Medico role sees only own appointments (AGENDA-07)
- New appointments default to 'agendado' (AGENDA-05)
- All responses use pagination envelope (D-12)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-crud/03-02-SUMMARY.md`
</output>
