---
phase: 03-core-crud
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-02"]
files_modified:
  - frontend/src/app/(dashboard)/pacientes/page.tsx
  - frontend/src/app/(dashboard)/pacientes/columns.tsx
  - frontend/src/app/(dashboard)/pacientes/patient-form.tsx
  - frontend/src/app/(dashboard)/pacientes/[id]/page.tsx
  - frontend/src/components/dashboard/patient/AppointmentHistory.tsx
  - frontend/src/components/dashboard/patient/WhatsAppTimeline.tsx
autonomous: true
requirements: [PAT-01, PAT-02, PAT-03, PAT-04, PAT-05]

must_haves:
  truths:
    - "Receptionist sees paginated patient list with search by name or phone"
    - "Receptionist can create a patient via slide-over form"
    - "Receptionist can edit patient data via slide-over form"
    - "Patient profile page shows header with patient info"
    - "Consultas tab shows appointment history table"
    - "Conversas WhatsApp tab shows chat bubble timeline"
  artifacts:
    - path: "frontend/src/app/(dashboard)/pacientes/page.tsx"
      provides: "Patient list page"
      contains: "DataTable"
    - path: "frontend/src/app/(dashboard)/pacientes/[id]/page.tsx"
      provides: "Patient profile page with tabs"
      contains: "Consultas"
    - path: "frontend/src/components/dashboard/patient/WhatsAppTimeline.tsx"
      provides: "Chat bubble timeline component"
      contains: "rounded-2xl"
  key_links:
    - from: "frontend/src/app/(dashboard)/pacientes/page.tsx"
      to: "/api/patients"
      via: "apiFetch for patient list and CRUD"
      pattern: "apiFetch.*patients"
    - from: "frontend/src/app/(dashboard)/pacientes/[id]/page.tsx"
      to: "/api/patients/{id}/appointments"
      via: "apiFetch for appointment history"
      pattern: "apiFetch.*appointments"
    - from: "frontend/src/app/(dashboard)/pacientes/[id]/page.tsx"
      to: "/api/patients/{id}/conversations"
      via: "apiFetch for WhatsApp history"
      pattern: "apiFetch.*conversations"
---

<objective>
Patient management pages: searchable list with DataTable, slide-over create/edit forms, and a patient profile page with tabbed appointment history and WhatsApp conversation timeline.

Purpose: Patient lookup and profile viewing are the most frequent receptionist actions. The WhatsApp timeline gives clinic staff visibility into AI conversations (core product value).

Output: Patient list page at /pacientes and profile page at /pacientes/[id] with two tabs.
</objective>

<execution_context>
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-crud/03-CONTEXT.md
@.planning/phases/03-core-crud/03-RESEARCH.md
@.planning/phases/03-core-crud/03-01-SUMMARY.md

@frontend/src/lib/api.ts
@frontend/src/lib/types.ts
@frontend/src/components/dashboard/DataTable.tsx
@frontend/src/components/dashboard/SlideOver.tsx
@frontend/src/components/dashboard/StatusBadge.tsx
@frontend/src/app/(dashboard)/layout.tsx

<interfaces>
From frontend/src/lib/types.ts:
```typescript
export interface Patient {
  id: string; phone: string; nome: string;
  data_nascimento: string | null; notas: string | null;
  total_consultas: number; created_at: string;
}
export interface Appointment { ... }
export interface ConversationMessage {
  id: string; role: "user" | "assistant"; content: string; created_at: string;
}
export interface PaginatedResponse<T> { items: T[]; total: number; page: number; per_page: number; }
```

From frontend/src/components/dashboard/DataTable.tsx:
```typescript
export function DataTable<TData>({
  columns, data, pageCount, pagination, onPaginationChange, isLoading
}: DataTableProps<TData>);
```

From frontend/src/components/dashboard/SlideOver.tsx:
```typescript
export function SlideOver({ open, onClose, title, children }: SlideOverProps);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patient list page — DataTable with search, slide-over create/edit forms</name>
  <files>
    frontend/src/app/(dashboard)/pacientes/page.tsx
    frontend/src/app/(dashboard)/pacientes/columns.tsx
    frontend/src/app/(dashboard)/pacientes/patient-form.tsx
  </files>
  <read_first>
    frontend/src/lib/api.ts
    frontend/src/lib/types.ts
    frontend/src/components/dashboard/DataTable.tsx
    frontend/src/components/dashboard/SlideOver.tsx
    frontend/src/app/(dashboard)/layout.tsx
  </read_first>
  <action>
1. Create `frontend/src/app/(dashboard)/pacientes/columns.tsx` — "use client"
   - Per D-05: Define ColumnDef<Patient>[] for @tanstack/react-table
   - Columns:
     - nome: header "Paciente", cell renders nome text
     - phone: header "Telefone"
     - data_nascimento: header "Nascimento", cell formats date with date-fns `format(parseISO(value), "dd/MM/yyyy")` or "-" if null
     - total_consultas: header "Consultas", cell renders count number
     - actions: header "", cell renders row action buttons — "Editar" (opens slide-over) and "Ver Perfil" (links to /pacientes/{id})
   - Export `columns` array and action callback types

2. Create `frontend/src/app/(dashboard)/pacientes/patient-form.tsx` — "use client"
   - Per D-08: react-hook-form + zod validation
   - Props: patient (Patient | null for create), onSubmit ((data) => Promise<void>), onCancel (() => void)
   - Fields:
     - nome: text input, required, min 2 chars
     - phone: text input, required, pattern for Brazilian phone (11 digits)
     - data_nascimento: date input, optional
     - notas: textarea, optional
   - Zod schema validates all fields
   - Submit button: "Cadastrar" (create) or "Salvar" (edit)
   - All labels in pt-BR

3. Create `frontend/src/app/(dashboard)/pacientes/page.tsx` — "use client"
   - Page title: "Pacientes" with "Novo Paciente" button (Plus icon from lucide-react)
   - Search input: debounced (300ms) text field. Searches on name or phone. Minimum 2 characters.
   - State: patients (Patient[]), pagination (page, pageCount), searchQuery, slideOverOpen, selectedPatient
   - Fetch: `apiFetch<PaginatedResponse<Patient>>("/api/patients?search=${query}&page=${page}&per_page=20")`
   - Refetch when search or page changes (useEffect dependency array)
   - Render DataTable with columns from columns.tsx
   - "Novo Paciente" button opens SlideOver with empty PatientForm
   - Row "Editar" action opens SlideOver with patient data pre-loaded
   - Row "Ver Perfil" navigates to /pacientes/{id} using `useRouter().push()`
   - On form submit (create): POST /api/patients, close slide-over, refetch
   - On form submit (edit): PUT /api/patients/{id}, close slide-over, refetch
   - Loading state: DataTable isLoading prop
   - Empty state: handled by DataTable "Nenhum registro encontrado"
  </action>
  <verify>
    <automated>cd /Users/mateuspires/Dev/agent-clinic/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/(dashboard)/pacientes/page.tsx` exists with "use client"
    - Page renders DataTable with patient columns
    - Page has search input with debounce
    - Page has "Novo Paciente" button that opens SlideOver
    - `columns.tsx` defines 5 columns: nome, phone, data_nascimento, total_consultas, actions
    - `patient-form.tsx` uses react-hook-form + zod, has 4 fields (nome, phone, data_nascimento, notas)
    - Page fetches from "/api/patients" with search and pagination params
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Patient list page renders searchable DataTable with create/edit slide-over forms. Navigation to profile page available per row.</done>
</task>

<task type="auto">
  <name>Task 2: Patient profile page — tabbed layout with appointment history and WhatsApp timeline</name>
  <files>
    frontend/src/app/(dashboard)/pacientes/[id]/page.tsx
    frontend/src/components/dashboard/patient/AppointmentHistory.tsx
    frontend/src/components/dashboard/patient/WhatsAppTimeline.tsx
  </files>
  <read_first>
    frontend/src/lib/api.ts
    frontend/src/lib/types.ts
    frontend/src/components/dashboard/StatusBadge.tsx
    frontend/src/components/ui/tabs.tsx
    frontend/src/app/globals.css
  </read_first>
  <action>
Per D-09: Tabbed layout with patient info header, tabs for "Consultas" and "Conversas WhatsApp". Each tab loads independently.
Per D-10: WhatsApp history as chat bubble timeline (read-only, WhatsApp-like UI).

1. Create `frontend/src/components/dashboard/patient/AppointmentHistory.tsx` — "use client"
   - Props: patientId (string)
   - Fetches `apiFetch<Appointment[]>("/api/patients/${patientId}/appointments")` on mount
   - Renders a table (simple, not DataTable — bounded data): columns for Data, Horario, Medico, Especialidade, Status
   - Status column uses StatusBadge component
   - Empty state: "Nenhuma consulta registrada"
   - Loading state: skeleton rows

2. Create `frontend/src/components/dashboard/patient/WhatsAppTimeline.tsx` — "use client"
   - Props: patientId (string)
   - Fetches `apiFetch<ConversationMessage[]>("/api/patients/${patientId}/conversations")` on mount
   - Per D-10 and research Pattern 6: Chat bubble timeline
   - Layout: flex flex-col gap-3 p-4 bg-gray-50 rounded-lg (chat background)
   - Each message bubble:
     - role === "user" (patient): align right, bg-green-500 text-white rounded-2xl rounded-br-sm
     - role === "assistant" (bot): align left, bg-white border border-gray-200 text-gray-800 rounded-2xl rounded-bl-sm
   - Each bubble shows content text + timestamp (HH:mm format) in smaller opacity text
   - Per specifics: should visually match the hero chat card from landing page
   - Empty state: "Nenhuma conversa registrada"
   - Loading state: 3 skeleton bubbles (alternating left/right)

3. Create `frontend/src/app/(dashboard)/pacientes/[id]/page.tsx` — "use client"
   - Read patient ID from route params
   - Fetch patient details: `apiFetch<Patient>("/api/patients/${id}")` — uses GET /api/patients/{id} added in plan 03-02
   - Patient info header: nome (large text, font-serif), phone, data_nascimento (formatted), notas
   - Back button: arrow left icon, navigates to /pacientes
   - Below header: shadcn Tabs component with two tabs:
     - "Consultas" tab: renders AppointmentHistory with patientId
     - "Conversas WhatsApp" tab: renders WhatsAppTimeline with patientId
   - Per D-09: tabs load independently (each component fetches its own data on mount, not when tab selected — or use lazy loading on tab switch)
   - Tab default: "Consultas"
  </action>
  <verify>
    <automated>cd /Users/mateuspires/Dev/agent-clinic/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/(dashboard)/pacientes/[id]/page.tsx` exists with "use client"
    - Page renders patient info header (nome, phone, data_nascimento)
    - Page has two tabs: "Consultas" and "Conversas WhatsApp"
    - `AppointmentHistory.tsx` fetches from /api/patients/{id}/appointments and renders table with StatusBadge
    - `WhatsAppTimeline.tsx` fetches from /api/patients/{id}/conversations and renders chat bubbles
    - Chat bubbles: user messages right-aligned green, assistant messages left-aligned white
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Patient profile page shows info header with tabbed Consultas and WhatsApp history. Chat timeline matches WhatsApp visual style per D-10.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Patient list page at /pacientes with DataTable and search
- Patient profile page at /pacientes/[id] with two tabs
- WhatsApp timeline uses green bubbles for patient, white for bot
</verification>

<success_criteria>
- Patient list with search by name/phone (PAT-01)
- Create patient via slide-over (PAT-02)
- Edit patient via slide-over (PAT-03)
- Profile page with appointment history tab (PAT-04)
- Profile page with WhatsApp conversation timeline tab (PAT-05)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-crud/03-05-SUMMARY.md`
</output>
