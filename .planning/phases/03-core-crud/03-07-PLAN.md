---
phase: 03-core-crud
plan: 07
type: execute
wave: 4
depends_on: ["03-04", "03-05", "03-06"]
files_modified: []
autonomous: false
requirements: [AGENDA-01, AGENDA-02, AGENDA-03, AGENDA-04, AGENDA-05, AGENDA-06, AGENDA-07, PAT-01, PAT-02, PAT-03, PAT-04, PAT-05, DOC-01, DOC-02, DOC-03, USER-01, USER-02, USER-03, USER-04, USER-05, API-01]

must_haves:
  truths:
    - "All CRUD pages render without errors"
    - "Calendar displays appointments in all 3 views"
    - "Patient profile shows appointment and WhatsApp tabs"
    - "Doctor availability grid is functional"
    - "User management CRUD works end to end"
    - "next build completes with zero errors"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of all Phase 3 CRUD functionality. Build check + visual walkthrough of all 4 entity management pages and the calendar.

Purpose: Catch integration issues, visual bugs, and broken wiring before marking the phase complete.

Output: Verified working CRUD system or list of issues to fix.
</objective>

<execution_context>
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-crud/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and type check</name>
  <files></files>
  <read_first>
    frontend/package.json
  </read_first>
  <action>
Run `next build` to verify the entire frontend compiles without errors:
```bash
cd /Users/mateuspires/Dev/agent-clinic/frontend && npm run build
```

If build fails:
1. Read error messages carefully
2. Fix TypeScript errors, import issues, or missing dependencies
3. Re-run build until clean

Also verify Python imports for all 4 API routers:
```bash
cd /Users/mateuspires/Dev/agent-clinic/agent-service
.venv/bin/python -c "
from src.api.patients import router as p
from src.api.appointments import router as a
from src.api.doctors import router as d
from src.api.users import router as u
from src.api.webhook import app
print(f'Patients routes: {len(p.routes)}')
print(f'Appointments routes: {len(a.routes)}')
print(f'Doctors routes: {len(d.routes)}')
print(f'Users routes: {len(u.routes)}')
print(f'App total routes: {len(app.routes)}')
print('All routers OK')
"
```
  </action>
  <verify>
    <automated>cd /Users/mateuspires/Dev/agent-clinic/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - No TypeScript errors in build output
    - All 4 Python routers import without error
    - webhook.py app includes all 4 CRUD routers
  </acceptance_criteria>
  <done>Frontend builds clean. All backend routers importable and mounted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual walkthrough of all CRUD pages</name>
  <files></files>
  <action>
Present the verification checklist to the user for manual testing of all CRUD pages. Prerequisites: FastAPI running on :8000, Next.js running on :3000, migration 004 applied, seed admin user exists.

Verification steps:

1. Agenda page (http://localhost:3000/agenda): Toggle Dia/Semana/Mes views, verify doctor filter dropdown visible as admin, click empty slot to open appointment form, create appointment and verify blue Agendado status block appears.

2. Patient list (http://localhost:3000/pacientes): Verify paginated table loads, search by name/phone, create new patient via slide-over, edit existing patient.

3. Patient profile (click Ver Perfil): Verify info header, Consultas tab with appointment history, Conversas WhatsApp tab with chat bubble timeline.

4. Doctor management (http://localhost:3000/medicos): Verify list table, create doctor, open Horarios grid, toggle availability cells and save.

5. User management (http://localhost:3000/usuarios): Verify list table, create user with role, toggle active/inactive, reset password.

6. Medico role isolation (login as medico): Verify /agenda hides doctor filter, only own appointments visible, /medicos and /usuarios not accessible.
  </action>
  <verify>User approves all 6 verification areas</verify>
  <done>User confirms all CRUD pages render correctly, forms work, and medico isolation is enforced.</done>
</task>

</tasks>

<verification>
- `npm run build` passes (zero errors)
- All 4 Python routers import and mount correctly
- Visual verification of all CRUD pages
</verification>

<success_criteria>
- Build compiles cleanly
- All 5 success criteria from ROADMAP Phase 3 are verifiable
- User approves visual walkthrough
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-crud/03-07-SUMMARY.md`
</output>
