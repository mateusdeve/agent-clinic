---
phase: 06-polish-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/components/dashboard/MobileNav.tsx]
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Drawer slides in smoothly from the left when hamburger is tapped"
    - "Drawer slides out smoothly to the left when backdrop or close button is tapped"
    - "Backdrop fades in/out instead of appearing/disappearing instantly"
  artifacts:
    - path: "frontend/src/components/dashboard/MobileNav.tsx"
      provides: "Animated slide-in drawer with CSS transitions"
      contains: "duration-300"
  key_links:
    - from: "MobileNav open state"
      to: "CSS translate-x classes"
      via: "clsx conditional class toggle on always-rendered DOM element"
      pattern: "open \\? .translate-x-0. : .-translate-x-full."
---

<objective>
Fix MobileNav drawer animation so it slides in/out smoothly instead of appearing instantly.

Purpose: UAT Test 1 reported "drawer opens but its not smooth. it just appears instantly." Root cause is conditional rendering ({open && ...}) that mounts/unmounts the drawer from the DOM, defeating CSS transitions.
Output: MobileNav.tsx with smooth slide animation via always-rendered DOM + CSS class toggling.
</objective>

<execution_context>
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/components/dashboard/MobileNav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix MobileNav drawer to use CSS-only animation</name>
  <files>frontend/src/components/dashboard/MobileNav.tsx</files>
  <action>
Rewrite the drawer rendering in MobileNav.tsx to fix the animation. The current code at line 57 uses `{open && (...)}` which mounts/unmounts the drawer from the DOM, making CSS transitions impossible. Apply these specific changes:

1. **Remove the `{open && (` conditional wrapper** (line 57 and closing line 107). The backdrop and drawer panel must ALWAYS be in the DOM.

2. **Backdrop (currently line 60-63):** Replace the static classes with open-conditional classes:
   - Always present: `fixed inset-0 z-40 md:hidden transition-opacity duration-300`
   - When open: `bg-black/40 opacity-100` — visible and clickable
   - When closed: `opacity-0 pointer-events-none` — invisible and non-interactive
   - Keep the `onClick={() => setOpen(false)}` handler

3. **Drawer panel (currently line 66-105):** Keep the existing structure but ensure transition classes work:
   - Base classes: `fixed inset-y-0 left-0 z-50 w-64 bg-white shadow-lg transform transition-transform duration-300 ease-in-out md:hidden`
   - When open: `translate-x-0`
   - When closed: `-translate-x-full`
   - The clsx conditional on line 69 already has the right logic (`open ? "translate-x-0" : "-translate-x-full"`), it just needs to actually execute when open=false (currently unreachable because the whole block is conditionally rendered).

4. **Ensure `duration-300` and `ease-in-out`** are on the drawer panel's className for smooth 300ms animation.

Keep everything else unchanged: hamburger button, getNavLinks function, drawer header, nav links, close button, all event handlers.
  </action>
  <verify>
    <automated>cd /Users/mateuspires/Dev/agent-clinic/frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>MobileNav drawer and backdrop are always in the DOM. Opening toggles translate-x-0 + opacity classes. Closing toggles -translate-x-full + opacity-0 + pointer-events-none. CSS transition-transform duration-300 ease-in-out produces smooth slide animation. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- MobileNav.tsx contains NO `{open && (` conditional rendering pattern
- MobileNav.tsx contains `duration-300` and `ease-in-out` for animation timing
- MobileNav.tsx contains `pointer-events-none` for closed backdrop state
- MobileNav.tsx contains `transition-opacity` on backdrop element
- MobileNav.tsx contains `transition-transform` on drawer panel element
</verification>

<success_criteria>
Drawer slides in from the left over 300ms when opened and slides out over 300ms when closed. Backdrop fades in/out. No instant mount/unmount flicker.
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-hardening/06-04-SUMMARY.md`
</output>
