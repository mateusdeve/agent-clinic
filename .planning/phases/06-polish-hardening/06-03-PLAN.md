---
phase: 06-polish-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - agent-service/tests/test_tenant_isolation.py
autonomous: false
requirements:
  - SC-3

must_haves:
  truths:
    - "RLS policies exist on all 8 tenant-scoped tables"
    - "A query with a non-matching tenant_id returns zero rows"
    - "Next.js frontend build passes with all Phase 6 changes"
  artifacts:
    - path: "agent-service/tests/test_tenant_isolation.py"
      provides: "Activated RLS and tenant isolation integration tests (no more xfail)"
      contains: "def test_rls_policies_exist"
  key_links:
    - from: "agent-service/tests/test_tenant_isolation.py"
      to: "PostgreSQL RLS policies"
      via: "pg_class relrowsecurity query"
      pattern: "relrowsecurity"
---

<objective>
Activate the xfail tenant isolation integration tests against the live database and run a full build verification checkpoint confirming all Phase 6 work.

Purpose: Two critical tenant isolation tests have been xfail since Phase 2. The RLS migrations are applied — these tests should pass. This plan removes xfail markers, runs the tests, and conducts a final visual verification of all responsive and error/loading work from Plans 01-02.
Output: Green tenant isolation tests, verified build, visual confirmation of responsive layout.
</objective>

<execution_context>
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-hardening/06-RESEARCH.md
@.planning/phases/06-polish-hardening/06-01-SUMMARY.md
@.planning/phases/06-polish-hardening/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate tenant isolation xfail tests</name>
  <files>agent-service/tests/test_tenant_isolation.py</files>
  <read_first>
    agent-service/tests/test_tenant_isolation.py,
    agent-service/tests/conftest.py
  </read_first>
  <action>
In `agent-service/tests/test_tenant_isolation.py`, make the following exact changes:

1. **Remove xfail from `test_rls_policies_exist`:**
   - Delete the line `@pytest.mark.xfail(reason="Plan 01 not yet executed")`
   - Keep the `@pytest.mark.tenant` decorator
   - The test body is already correct — it checks `relrowsecurity` on 8 tables

2. **Remove xfail from `test_tenant_isolation_across_clinics`:**
   - Delete the line `@pytest.mark.xfail(reason="Plan 01 not yet executed")`
   - Keep the `@pytest.mark.tenant` decorator
   - The test body is already correct — it sets a non-existent tenant UUID and checks count=0

3. **Remove xfail from `test_get_db_for_tenant_sets_session_var`:**
   - Delete the line `@pytest.mark.xfail(reason="Plan 02 not yet executed")`
   - Keep the `@pytest.mark.tenant` decorator
   - This test was already XPASS (passing despite xfail), so removing xfail is safe

After removing xfail decorators, run the tests. If `DATABASE_URL` is not set or the DB is unreachable, the `db_conn` fixture in conftest.py will skip the tests automatically (this is expected behavior in CI). The tests themselves are correct — only the xfail markers need removal.

Run: `cd /Users/mateuspires/Dev/agent-clinic/agent-service && python3 -m pytest tests/test_tenant_isolation.py -v --tb=short`

If tests pass: done. If tests fail because DB is unreachable (SKIPPED), that is acceptable — the xfail removal is still correct. If tests fail because RLS is not enabled, note the failure but do NOT re-add xfail — the migration from Phase 2 should have applied RLS.
  </action>
  <verify>
    <automated>cd /Users/mateuspires/Dev/agent-clinic/agent-service && python3 -m pytest tests/test_tenant_isolation.py -v --tb=short 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - test_tenant_isolation.py does NOT contain the string `xfail`
    - test_tenant_isolation.py contains `def test_rls_policies_exist` without xfail decorator above it
    - test_tenant_isolation.py contains `def test_tenant_isolation_across_clinics` without xfail decorator above it
    - test_tenant_isolation.py contains `def test_get_db_for_tenant_sets_session_var` without xfail decorator above it
    - All three test functions retain `@pytest.mark.tenant` decorator
    - pytest run completes (pass or skip, not error)
  </acceptance_criteria>
  <done>All three tenant isolation tests run without xfail markers. Tests either pass (live DB) or skip (no DB) — no more xfail masking.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 6 work</name>
  <files>none</files>
  <action>
Human visual verification of all Phase 6 deliverables across mobile, tablet, error, and loading scenarios.
What was built:
- Plan 01: MobileNav drawer, responsive DataTable, mobile inbox layout
- Plan 02: global-error.tsx, dashboard error.tsx, loading.tsx skeletons, ErrorAlert
- Plan 03 Task 1: Activated tenant isolation tests (xfail removed)
  </action>
  <how-to-verify>
    1. Start the dev server: `cd frontend && npm run dev`
    2. Open Chrome DevTools, toggle device toolbar

    **Mobile (375px iPhone SE):**
    3. Navigate to /home — verify hamburger icon visible in header, sidebar hidden
    4. Tap hamburger — verify drawer opens with correct nav links for your role
    5. Tap a nav link (e.g., Pacientes) — verify drawer closes, page loads, skeleton appears during transition
    6. On /pacientes — verify table scrolls horizontally (swipe left/right), no page overflow
    7. Navigate to /whatsapp — verify conversation list shows, no thread visible
    8. Tap a conversation — verify thread appears, list hides, back arrow visible
    9. Tap back arrow — verify list reappears

    **Tablet (768px iPad Mini):**
    10. Verify sidebar is visible (md breakpoint), hamburger hidden
    11. Tables render without overflow

    **Error states:**
    12. In Chrome DevTools Network tab, block requests to your API (right-click -> Block request URL matching `*/api/*`)
    13. Refresh /home — verify error boundary appears with "Tentar novamente" button (or inline error banner)

    **Loading states:**
    14. In DevTools, throttle network to Slow 3G
    15. Navigate between dashboard pages — verify skeleton loading appears during transitions
  </how-to-verify>
  <verify>
    <automated>cd /Users/mateuspires/Dev/agent-clinic/frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>User approves responsive layout on mobile/tablet, error boundaries show recovery UI, loading skeletons appear during transitions</done>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd /Users/mateuspires/Dev/agent-clinic/agent-service && python3 -m pytest tests/test_tenant_isolation.py -v` — no xfail, tests pass or skip
- `cd /Users/mateuspires/Dev/agent-clinic/frontend && npx next build` exits 0
- Visual verification of responsive, error, and loading behavior
</verification>

<success_criteria>
- Tenant isolation tests activated (no xfail markers remain)
- All Phase 6 visual behavior verified by user on mobile and tablet viewports
- Error boundaries show recovery UI on failures
- Loading skeletons appear during route transitions
- Next.js build passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-hardening/06-03-SUMMARY.md`
</output>
