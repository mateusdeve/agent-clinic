---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/app/globals.css
  - frontend/src/app/layout.tsx
  - frontend/src/app/(landing)/layout.tsx
  - frontend/src/app/(landing)/page.tsx
  - frontend/src/lib/utils.ts
  - frontend/components.json
  - frontend/.env.local
  - frontend/tsconfig.json
  - frontend/postcss.config.mjs
autonomous: true
requirements: [LAND-04]
must_haves:
  truths:
    - "Next.js dev server starts without errors on localhost:3000"
    - "Tailwind utility classes bg-green-500 and font-serif resolve to the correct design tokens"
    - "DM Serif Display and DM Sans fonts load without layout shift via next/font"
    - "Root URL / renders from (landing) route group, not app/page.tsx"
    - "shadcn Button component imports and renders without error"
  artifacts:
    - path: "frontend/package.json"
      provides: "Next.js + Tailwind 4 + shadcn dependencies"
      contains: "next"
    - path: "frontend/src/app/globals.css"
      provides: "Tailwind 4 design tokens via @theme"
      contains: "--color-green-500"
    - path: "frontend/src/app/layout.tsx"
      provides: "Root layout with DM fonts and pt-BR lang"
      contains: "DM_Serif_Display"
    - path: "frontend/src/app/(landing)/page.tsx"
      provides: "Landing page entry point"
    - path: "frontend/components.json"
      provides: "shadcn configuration"
      contains: "shadcn"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/app/globals.css"
      via: "import globals.css"
      pattern: "import.*globals.css"
    - from: "frontend/src/app/globals.css"
      to: "Tailwind utility classes"
      via: "@theme directive"
      pattern: "@theme"
---

<objective>
Scaffold the Next.js project inside frontend/, configure Tailwind CSS 4 design tokens matching site.html's green palette and DM typography, initialize shadcn/ui, and set up route groups for (landing), (auth), and (dashboard).

Purpose: Establish the foundation that all landing page components will build upon — fonts, colors, layout shell, and component library.
Output: A running Next.js app at localhost:3000 with correct design tokens, fonts, and route group structure.
</objective>

<execution_context>
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mateuspires/Dev/agent-clinic/.planning/PROJECT.md
@/Users/mateuspires/Dev/agent-clinic/.planning/ROADMAP.md
@/Users/mateuspires/Dev/agent-clinic/.planning/STATE.md
@/Users/mateuspires/Dev/agent-clinic/.planning/phases/01-foundation/01-RESEARCH.md
@/Users/mateuspires/Dev/agent-clinic/.planning/phases/01-foundation/01-UI-SPEC.md
@/Users/mateuspires/Dev/agent-clinic/frontend/site.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project and configure design system</name>
  <files>
    frontend/package.json
    frontend/src/app/globals.css
    frontend/src/app/layout.tsx
    frontend/src/app/(landing)/layout.tsx
    frontend/src/app/(landing)/page.tsx
    frontend/src/app/(auth)/page.tsx
    frontend/src/app/(dashboard)/page.tsx
    frontend/src/lib/utils.ts
    frontend/components.json
    frontend/.env.local
    frontend/postcss.config.mjs
    frontend/tsconfig.json
  </files>
  <read_first>
    /Users/mateuspires/Dev/agent-clinic/frontend/site.html
    /Users/mateuspires/Dev/agent-clinic/.planning/phases/01-foundation/01-RESEARCH.md
    /Users/mateuspires/Dev/agent-clinic/.planning/phases/01-foundation/01-UI-SPEC.md
  </read_first>
  <action>
    **Step 1 — Scaffold Next.js project:**

    The frontend/ directory currently contains only site.html. Back up site.html, then run from the frontend/ directory:

    ```bash
    cd /Users/mateuspires/Dev/agent-clinic/frontend
    mv site.html ../site.html.bak
    npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --yes
    mv ../site.html.bak ./site.html
    ```

    **Step 2 — Delete tailwind.config.ts if generated (Tailwind 4 does not use it):**

    ```bash
    rm -f tailwind.config.ts tailwind.config.js
    ```

    **Step 3 — Configure postcss.config.mjs for Tailwind 4:**

    Ensure postcss.config.mjs uses `@tailwindcss/postcss`:

    ```js
    const config = {
      plugins: {
        "@tailwindcss/postcss": {},
      },
    };
    export default config;
    ```

    If the scaffolder generated a different postcss config, replace it with the above.

    **Step 4 — Replace globals.css with Tailwind 4 design tokens:**

    Replace `src/app/globals.css` entirely with:

    ```css
    @import "tailwindcss";

    @theme {
      /* Green palette (from site.html :root) */
      --color-green-50: #edf7f0;
      --color-green-100: #d0edd8;
      --color-green-400: #4caf7d;
      --color-green-500: #2e9e60;
      --color-green-600: #1e7d48;
      --color-green-900: #0d2e1a;

      /* Neutral palette (from site.html :root) */
      --color-off-white: #f5f7f5;
      --color-gray-200: #e8ece9;
      --color-gray-400: #9aaa9e;
      --color-gray-600: #5a6b5f;
      --color-gray-800: #2a352d;

      /* Typography — CSS variables injected by next/font */
      --font-serif: var(--font-dm-serif), "DM Serif Display", serif;
      --font-sans: var(--font-dm-sans), "DM Sans", sans-serif;
    }

    /* Prevent horizontal overflow on all viewports */
    html, body {
      overflow-x: hidden;
    }
    ```

    Do NOT keep any default Tailwind styles, dark mode variables, or the scaffolded CSS. The above is the complete file.

    **Step 5 — Configure root layout with fonts:**

    Replace `src/app/layout.tsx` with:

    ```tsx
    import type { Metadata } from "next";
    import { DM_Serif_Display, DM_Sans } from "next/font/google";
    import "./globals.css";

    const dmSerif = DM_Serif_Display({
      weight: ["400"],
      style: ["normal", "italic"],
      subsets: ["latin"],
      variable: "--font-dm-serif",
    });

    const dmSans = DM_Sans({
      weight: ["300", "400", "500", "600"],
      subsets: ["latin"],
      variable: "--font-dm-sans",
    });

    export const metadata: Metadata = {
      title: "MedIA — Atendente de IA para Clinicas",
      description: "Agendamentos, confirmacoes, lembretes e duvidas — tudo resolvido por IA, 24 horas por dia.",
    };

    export default function RootLayout({
      children,
    }: Readonly<{
      children: React.ReactNode;
    }>) {
      return (
        <html lang="pt-BR" className={`${dmSerif.variable} ${dmSans.variable}`}>
          <body className="font-sans text-gray-800 bg-white">{children}</body>
        </html>
      );
    }
    ```

    **Step 6 — Create route groups:**

    Delete the scaffolded `src/app/page.tsx` (it conflicts with the landing route group).

    Create `src/app/(landing)/layout.tsx`:
    ```tsx
    export default function LandingLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return <>{children}</>;
    }
    ```

    Create `src/app/(landing)/page.tsx` as a placeholder:
    ```tsx
    export default function LandingPage() {
      return (
        <main className="min-h-screen">
          <h1 className="font-serif text-green-900 text-4xl p-12">
            MedIA — Landing Page
          </h1>
          <p className="text-gray-600 p-12">Scaffold working. Design tokens active.</p>
        </main>
      );
    }
    ```

    Create stub route groups for future phases:

    `src/app/(auth)/page.tsx`:
    ```tsx
    export default function AuthPage() {
      return <p>Auth — Phase 2</p>;
    }
    ```

    `src/app/(dashboard)/page.tsx`:
    ```tsx
    export default function DashboardPage() {
      return <p>Dashboard — Phase 3</p>;
    }
    ```

    **Step 7 — Initialize shadcn/ui:**

    ```bash
    cd /Users/mateuspires/Dev/agent-clinic/frontend
    npx shadcn@latest init -y
    ```

    After init, verify `components.json` exists and has `"css": "src/app/globals.css"`. If shadcn overwrote globals.css, restore the @theme tokens from Step 4 (merge any shadcn CSS variables into the file while keeping the @theme block intact).

    Then add the Button component:
    ```bash
    npx shadcn@latest add button -y
    ```

    **Step 8 — Create .env.local:**

    ```
    NEXT_PUBLIC_WHATSAPP_NUMBER=5511999999999
    ```

    **Step 9 — Verify the dev server starts:**

    ```bash
    cd /Users/mateuspires/Dev/agent-clinic/frontend
    npm run build
    ```

    Build must complete with zero errors.
  </action>
  <verify>
    <automated>cd /Users/mateuspires/Dev/agent-clinic/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0 and no errors
    - File `frontend/src/app/globals.css` contains the string `--color-green-500: #2e9e60`
    - File `frontend/src/app/globals.css` contains the string `@theme`
    - File `frontend/src/app/globals.css` contains `@import "tailwindcss"`
    - File `frontend/src/app/layout.tsx` contains `DM_Serif_Display`
    - File `frontend/src/app/layout.tsx` contains `DM_Sans`
    - File `frontend/src/app/layout.tsx` contains `lang="pt-BR"`
    - File `frontend/src/app/layout.tsx` contains `--font-dm-serif`
    - File `frontend/src/app/(landing)/page.tsx` exists and exports a default function
    - File `frontend/components.json` exists
    - File `frontend/src/components/ui/button.tsx` exists
    - File `frontend/.env.local` contains `NEXT_PUBLIC_WHATSAPP_NUMBER`
    - File `frontend/tailwind.config.ts` does NOT exist (Tailwind 4 CSS-first)
    - File `frontend/src/app/page.tsx` does NOT exist (removed to avoid route conflict)
  </acceptance_criteria>
  <done>
    Next.js project scaffolded with Tailwind 4 design tokens (green palette, DM fonts), shadcn initialized with Button component, route groups (landing/auth/dashboard) created, and `npm run build` passes with zero errors.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `grep -q "@theme" frontend/src/app/globals.css` returns 0
- `grep -q "DM_Serif_Display" frontend/src/app/layout.tsx` returns 0
- `test -f frontend/components.json` returns 0
- `test ! -f frontend/tailwind.config.ts` returns 0
- `test -f frontend/src/app/(landing)/page.tsx` returns 0
</verification>

<success_criteria>
The Next.js project is fully scaffolded with Tailwind 4 CSS-first design tokens matching site.html's color palette and typography. shadcn/ui is initialized. Route groups exist for landing, auth, and dashboard. The build passes cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
