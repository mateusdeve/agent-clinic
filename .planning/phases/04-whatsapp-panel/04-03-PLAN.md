---
phase: 04-whatsapp-panel
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified: []
autonomous: false
requirements: [WPP-01, WPP-02, WPP-03, WPP-04, WPP-05, WPP-06, WPP-07, WPP-08, WPP-09, WPP-10, WPP-11, API-04]

must_haves:
  truths:
    - "Next.js build succeeds with zero TypeScript errors"
    - "Backend Python imports resolve without errors"
    - "All 12 phase requirements visually verified in running application"
  artifacts: []
  key_links: []
---

<objective>
Build verification and visual walkthrough of the complete WhatsApp inbox.

Purpose: Confirms that all components integrate correctly end-to-end before marking Phase 4 complete. Catches build issues, import errors, and visual regressions.

Output: Verified working WhatsApp inbox. Phase 4 ready for completion.
</objective>

<execution_context>
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateuspires/Dev/agent-clinic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-whatsapp-panel/04-01-SUMMARY.md
@.planning/phases/04-whatsapp-panel/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification — frontend TypeScript + backend Python imports</name>
  <files></files>
  <read_first>
    frontend/package.json,
    agent-service/requirements.txt
  </read_first>
  <action>
1. Run Next.js build:
   ```bash
   cd frontend && npx next build
   ```
   Must complete with zero errors. If TypeScript errors found, fix them in the relevant files.

2. Run backend import verification:
   ```bash
   cd agent-service && python -c "
   from src.api.socketio_server import sio, socket_app, redis_client
   from src.api.conversations import router
   from src.api.webhook import app
   print('All imports OK')
   print('Conversations routes:', [r.path for r in router.routes])
   "
   ```
   Must print all routes without import errors.

3. Run test collection:
   ```bash
   cd agent-service && pytest tests/test_conversations.py --collect-only -q
   ```
   Must collect 6 tests without errors.

4. If any errors found in steps 1-3, fix them and re-run until all pass.
  </action>
  <verify>
    <automated>cd /Users/mateuspires/Dev/agent-clinic/frontend && npx next build 2>&1 | grep -E "(error|Error|Build)" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Next.js build completes with "Compiled successfully" or similar success message
    - Zero TypeScript errors in build output
    - Backend Python imports resolve without ImportError or ModuleNotFoundError
    - pytest collects 6 test stubs from test_conversations.py
  </acceptance_criteria>
  <done>Frontend and backend build clean. All modules importable. Tests collectible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual walkthrough of WhatsApp inbox</name>
  <files></files>
  <action>
Present the user with the verification checklist below. Start both servers if not already running:
- Backend: `cd agent-service && python run_api.py` (port 8000)
- Frontend: `cd frontend && npm run dev` (port 3000)
Wait for user to verify each area and provide approval or describe issues.
  </action>
  <verify>
    <automated>echo "Checkpoint: requires human verification"</automated>
  </verify>
  <what-built>
Complete WhatsApp inbox with real-time Socket.IO, 3-column layout, conversation list with search/filter, message thread with chat bubbles, patient sidebar, and takeover controls.
  </what-built>
  <how-to-verify>
Start both servers:
1. Backend: `cd agent-service && python run_api.py` (should start on port 8000)
2. Frontend: `cd frontend && npm run dev` (should start on port 3000)

Then verify these areas:

**Area 1 — Inbox page loads (WPP-01, D-04):**
- Log in as admin or recepcionista
- Click "WhatsApp" in the sidebar
- Verify 3-column layout is visible: conversation list (left), empty thread area (center), no sidebar (right, appears on selection)

**Area 2 — Conversation list (WPP-03, WPP-05, D-05):**
- Conversations appear sorted by latest message
- Each shows patient name (or phone), last message preview, status badge (IA/Humano)
- Search input filters by name or phone
- Filter tabs work: Todas | IA Ativa | Humano | Resolvida

**Area 3 — Message thread (WPP-02, WPP-04, D-06):**
- Click a conversation
- Full message history loads with chat bubbles (green right = patient, white left = bot)
- If someone sends a WhatsApp message, it appears in real-time (WPP-04)

**Area 4 — Patient sidebar (WPP-06):**
- Right column shows patient name, phone, recent appointments
- "Ver perfil completo" link navigates to patient profile

**Area 5 — Takeover flow (WPP-07, WPP-08, WPP-09, WPP-10, WPP-11):**
- Click "Assumir" button — status changes to "Humano" with your name
- Send bar becomes active — type a message and press Enter
- Message appears in thread and is sent to patient via WhatsApp
- Click "Devolver para IA" — status changes back to "IA Ativa"
- Another browser tab (if open) sees the status change immediately (WPP-11)

**Area 6 — Socket.IO connection (API-04):**
- Check browser console — no Socket.IO connection errors
- Connection indicator (green dot) visible in inbox
  </how-to-verify>
  <done>User approved all 6 verification areas. Phase 4 WhatsApp Panel complete.</done>
  <resume-signal>Type "approved" or describe issues found in each area</resume-signal>
</task>

</tasks>

<verification>
All 12 phase requirements verified via build check + visual walkthrough:
- API-04: Socket.IO server mounts and accepts connections
- WPP-01: Conversation list loads in real time
- WPP-02: Full message history visible
- WPP-03: Status indicators visible (IA/Humano/Resolvida)
- WPP-04: New messages appear without refresh
- WPP-05: Search filters conversations
- WPP-06: Patient sidebar shows info
- WPP-07: Takeover button works
- WPP-08: Human can send messages
- WPP-09: Handback button works
- WPP-10: AI stops responding during takeover
- WPP-11: All users see status changes
</verification>

<success_criteria>
- Build passes with zero errors (frontend + backend)
- User approves visual walkthrough of all 6 verification areas
- Phase 4 ready for completion
</success_criteria>

<output>
After completion, create `.planning/phases/04-whatsapp-panel/04-03-SUMMARY.md`
</output>
